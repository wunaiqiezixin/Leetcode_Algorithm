# 209. 长度最小的子数组

给定一个含有 n 个正整数的数组和一个正整数 target 。

找出该数组中满足其总和大于等于 target 的长度最小的 子数组 [numsl, numsl+1, ..., numsr-1, numsr] ，并返回其长度。如果不存在符合条件的子数组，返回 0 。

示例 1：

输入：target = 7, nums = [2,3,1,2,4,3]

输出：2

解释：子数组 [4,3] 是该条件下的长度最小的子数组。

示例 2：

输入：target = 4, nums = [1,4,4]

输出：1

示例 3：

输入：target = 11, nums = [1,1,1,1,1,1,1,1]

输出：0
 

提示：

* 1 <= target <= 109
* 1 <= nums.length <= 105
* 1 <= nums[i] <= 104
 

进阶：

如果你已经实现 O(n) 时间复杂度的解法, 请尝试设计一个 O(n log(n)) 时间复杂度的解法。

## 暴力枚举
* 用 i 表示子数组的起始位置，用 j 表示子数组的终止位置
* i 从 0 到 numsSize - 1, j 从 i 到 numsSize - 1
* length,minLength 记录当前符合条件的子数组的长度和最小长度
* 初始化minLength = numsSize + 1;

### ***C:***
```C
int minSubArrayLen(int target, int* nums, int numsSize) {
    int minLength = numsSize + 1;
    int i,j,length,Sum;

    for (i = 0;i < numsSize;i++)
    {
        Sum = 0;
        for (j = i;j < numsSize;j++)
        {
            Sum += nums[j];
            if (Sum >= target)
            {
                length = j - i + 1;
                if (minLength > length)
                {
                    minLength = length;
                }
            }
        }
    }
    return minLength == numsSize + 1 ? 0 : minLength;
}
```

## 滑动窗口
* 在暴力枚举的基础上进行优化，只进行一次for循环
* 分别用标识符i,j表示连续子数组的起始，终止位置
* 初始化i，j均为0
* j往后遍历，直到子数组的和Sum>=target
* 保持j不动，Sum -= nums[i++] , 直到Sum<target
* 在过程中注意记录最小长度minLength = min(minLength,j-i+1)

### ***C:***
```C
int minSubArrayLen(int target, int* nums, int numsSize) {
    int i = 0,minLength = numsSize + 1,Sum = 0;
    int j,length;
    for (j = 0;j < numsSize;j++)
    {
        Sum += nums[j];
        while (Sum >= target)
        {
            length = j - i + 1;
            if (minLength > length)
            {
                minLength = length;
            }
            Sum -= nums[i++];
        }
    }
    return minLength != numsSize + 1 ? minLength : 0;
}
```
