# 2755:神奇的口袋
[题目链接](http://bailian.openjudge.cn/practice/2755/)

---
## 🍀递归解法
#### 🧠思路
将递归和树结构联系在一起  
递归分两种  
1. 有返回值的递归  
分解问题
2. 无返回值的递归  
遍历(树)

本题属于第一种，我们需要将问题进行分解  

- 函数参数与返回值  
```C
int ways(int k, int w)
//表示从前k个物体中凑出体积w的取法
```
- 终止条件：当w为0时，返回1(先判断)；当k为0时，返回0(后判断)
```C
if (w == 0)/*前*/
	return 1;//一个都不取也是一种取法
if (k == 0)/*后*/
	return 0;
```
- 分解问题
```C
//分解问题：a1:取第k个物体 ； a2:不取第k个物体
int a1 = w - V[k] >= 0 ? ways(k - 1, w - V[k]) : 0;
int a2 = ways(k - 1, w);
return a1 + a2;
```
完整代码
### ***C:***
```C
#define _CRT_SECURE_NO_WARNINGS
#include<stdio.h>
int V[30];//V[i]表示第i个物体的体积

int ways(int k, int w)//表示从前k个物体中凑出体积w的取法
{
	//终止条件
	if (w == 0)/*前*/
		return 1;//一个都不取也是一种取法
	if (k == 0)/*后*/
		return 0;
	//分解问题：a1:取第k个物体 ； a2:不取第k个物体
	int a1 = w - V[k] >= 0 ? ways(k - 1, w - V[k]) : 0;//注意这里不是 > , 是 >=
	int a2 = ways(k - 1, w);
	return a1 + a2;
}

int main(void)
{
	int N;
	scanf("%d", &N);
	for (int i = 1; i <= N; i++)
		scanf("%d", &V[i]);
	printf("%d", ways(N, 40));
}
```
## 🚀动规解法
#### 🧠思路
