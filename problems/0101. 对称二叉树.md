# 101. 对称二叉树
[力扣题目链接](https://leetcode.cn/problems/symmetric-tree/description/)

---
## 🧠思路
本题有两个关键点  
1. 只能用后序遍历
要判断是否是对称的二叉树，本质上是比较根节点的左右两棵子树的节点  
因此，我们要遍历的是两棵树，要先知道了左右子树是否相等，才能判断根节点所在的树是否是对称的  
一棵树的遍历顺序：左-右-中  
另一颗树的遍历顺序：右-左-中  
2. 要比较的是内侧节点和外侧节点
内侧节点:左节点的右节点vs右节点的左节点  
外侧节点：左节点的左节点vs右节点的右节点  
## 🍀递归法
1. 要比较的是左右子树，因此函数的参数为左右节点
```C
bool compare(struct TreeNode* left, struct TreeNode* right)
```
2. 终止条件
```C
if (left == NULL && right != NULL) return false;
else if (left != NULL && right == NULL) return false;
else if (left == NULL && right == NULL) return true;
else if (left->val != right->val) return false;
```
3. 递归
```C
bool inside = compare(left->right, right->left);/*比较内侧节点是否相同*/
bool outside = compare(left->left, right->right);/*比较外侧节点是否相同*/
return inside && outside;//最后判断是否对称 (中)
```
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
 //递归法
//比较的是左右子树是否可以翻转，传入根节点的左右子树
bool compare(struct TreeNode* left, struct TreeNode* right)
{
    //判断边界条件
    if (left == NULL && right != NULL) return false;
    else if (left != NULL && right == NULL) return false;
    else if (left == NULL && right == NULL) return true;
    else if (left->val != right->val) return false;
    //下一层 (左-右)/(右-左)
    bool inside = compare(left->right, right->left);/*比较内侧节点是否相同*/
    bool outside = compare(left->left, right->right);/*比较外侧节点是否相同*/
    return inside && outside;//最后判断是否对称 (中)
}
bool isSymmetric(struct TreeNode* root) {
    if (root == NULL) return true;
    return compare(root->left, root->right);
}
```
## 📝迭代法--队列
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
//迭代法：
bool isSymmetric(struct TreeNode* root) {
    //队列
    struct TreeNode** que = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 1000);
    int front = 0, rear = 0;
    //根节点的左右节点入队
    if (root == NULL) return true;
    que[rear++] = root->left;
    que[rear++] = root->right;
    //循环迭代
    while (front < rear)
    {
        //左右节点出队
        struct TreeNode* left = que[front++];
        struct TreeNode* right = que[front++];
        //如果都为空
        if (!left && !right)
            continue;
        //如果有且仅有一个为空，或值不相等
        if ((!left && right) || (left && !right) || (left->val != right->val))
            return false;
        //节点入队(注意顺序)
        que[rear++] = left->left;//左节点的左节点
        que[rear++] = right->right;//右节点的右节点
        que[rear++] = left->right;//左节点的右节点
        que[rear++] = right->left;//右节点的左节点
    }
    return true;
}
```
## 📚迭代法--栈
其实，迭代法就是用一个数据结构将要比较的节点**两两按顺序**加入  
所以本题用栈或队列均可，只要保证内侧和内侧比较，外侧和外侧比较
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
bool isSymmetric(struct TreeNode* root) {
    //栈
    struct TreeNode** stack = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 1000);
    int stackTop = 0;
    //根节点的左右节点入栈
    if (root == NULL) return true;
    stack[stackTop++] = root->left;
    stack[stackTop++] = root->right;
    //循环迭代
    while (stackTop)
    {
        //要比较的两个节点出栈
        struct TreeNode* right = stack[--stackTop];
        struct TreeNode* left = stack[--stackTop];
        //如果都为空
        if (!left && !right) 
            continue;
        //如果有且仅有一个为空，或值不等
        if ((left && !right) || (!left && right) || (left->val != right->val))
            return false;
        //节点入栈，注意两两要匹配
        stack[stackTop++] = left->left;
        stack[stackTop++] = right->right;
        stack[stackTop++] = left->right;
        stack[stackTop++] = right->left;
    }
    return true;
}
```
