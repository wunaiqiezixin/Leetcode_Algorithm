# 102. 二叉树的层序遍历
[力扣题目链接](https://leetcode.cn/problems/binary-tree-level-order-traversal/description/)
---
## 🧠分析
我们关注题目本身(C语言实现)    
本题要求我们逐层的遍历二叉树，并且不是用一维数组存结果  
也就是说，我们要算出一共有多少层，每一层有多少个节点，并且还要分别将每一层的节点的值存入一个一维的数组，最后返回一个二维的数组    
这就是本题的难度所在  
## 📝思路
毫无疑问，用队列  
但是怎么实现就有讲究了  
我是这样做的：  
用数组实现一个队列，队列有 front、rear、queSize 三个可以维护的属性  
我们一般维护 front 和 queSize ,这样的话 rear = front + queSize ,每次执行入队操作时将尾指针算出来即可  
但是，  
在本题，我维护 front 和 rear ， 然后 队列的长度 queSize = rear - front  
我用变量 size 表示每一层的节点数量  
每记录完一层节点的值后， 我都可以很方便的算出下一层节点的数量 size = rear - front
### 完整代码
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
/**
 * Return an array of arrays of size *returnSize.
 * The sizes of the arrays are returned as *returnColumnSizes array.
 * Note: Both returned array and *columnSizes array must be malloced, assume caller calls free().
 */
int** levelOrder(struct TreeNode* root, int* returnSize, int** returnColumnSizes) {
    //分配足够的空间
    int** ret = (int**)malloc(sizeof(int*) * 2000);
    *returnColumnSizes = (int*)malloc(sizeof(int) * 2000);
    *returnSize = 0;//层数
    //定义一个队列，维护头尾指针
    struct TreeNode** que = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 2000);
    int front = 0, rear = 0;
    int size = 0;//每一层的节点数
    //根节点入队
    if (root == NULL) return ret;
    que[rear++] = root;
    while (front < rear)//只要队列不为空
    {
        //计算当前这一层的节点数量
        (*returnColumnSizes)[(*returnSize)] = size = rear - front;
        //临时数组，存放当前这一层的值
        int* tmp = (int*)malloc(sizeof(int) * size);
        int cnt = 0;
        //将一整层的节点值加入数组，同时将下一层的节点入队
        while (size--)
        {
            struct TreeNode* cur = que[front++];
            tmp[cnt++] = cur->val;
            //将下一层的节点从左到右入队
            if (cur->left)
                que[rear++] = cur->left;
            if (cur->right)
                que[rear++] = cur->right;
            if (!size)//遍历完一整层
            {
                ret[(*returnSize)++] = tmp;
            }
        }
    }
    return ret;
}
```
