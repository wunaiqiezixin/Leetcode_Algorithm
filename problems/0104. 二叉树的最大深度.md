# 104. 二叉树的最大深度
[力扣题目链接](https://leetcode.cn/problems/maximum-depth-of-binary-tree/description/)

---
## 🍀递归
要求二叉树的深度，我们一般用前序遍历  
要求二叉树的高度，我们一般用后序遍历  
对于本题，二叉树的最大深度就等于它的高度，所以前序后序均可
### 后序遍历
1. 确定函数的返回与参数
本题要求的是最大深度，所以返回即为最大深度，我们传入一个节点
```C
int maxDepth(struct TreeNode* root)
```
2. 边界
当传入的节点为空时，高度为0
```C
if (root == NULL) return 0;
```
3. 单层递归逻辑
我们要后序遍历二叉树，所以遵循：左-右-中的遍历逻辑
```C
/*左*/
int leftMax = maxDepth(root->left);
/*右*/
int rightMax = maxDepth(root->right);
/*中*/
return 1 + max(leftMax, rightMax);
```
### 完整代码
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
#define max(a, b) ((a) > (b) ? (a) : (b))
//递归：后序遍历(左-右-中)
int maxDepth(struct TreeNode* root) {
    if (root == NULL) return 0;
    /*左*/
    int leftMax = maxDepth(root->left);
    /*右*/
    int rightMax = maxDepth(root->right);
    /*中*/
    return 1 + max(leftMax, rightMax);
}
```
### 前序遍历
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
int result;
void getDepth(struct TreeNode* node, int depth)
{
    /*中*/
    result = depth > result ? depth : result;
    if (node == NULL) return;
    /*左*/
    if (node->left)
    {
        depth++;
        getDepth(node->left, depth);
        depth--;//回溯
    }
    /*右*/
    if (node->right)
    {
        depth++;
        getDepth(node->right, depth);
        depth--;//回溯
    }
    return;
}
int maxDepth(struct TreeNode* root) {
    result = 0;
    if (root == NULL) return result;
    getDepth(root, 1);
    return result;
}
```

## 📝迭代
层序遍历，可以直接套模板
### 完整代码
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */

int maxDepth(struct TreeNode* root) {
    //队列
    struct TreeNode** que = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 100000);
    int front = 0, rear = 0;
    int size = 0;//层数
    if (root == NULL) return 0;
    que[rear++] = root;
    while (front < rear)
    {
        size++;
        int len = rear - front;
        while (len--)
        {
            struct TreeNode* cur = que[front++];
            if (cur->left)
                que[rear++] = cur->left;
            if (cur->right)
                que[rear++] = cur->right;
        }
    }    
    return size;
}
```
