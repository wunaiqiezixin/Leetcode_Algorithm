# 105. 从前序与中序遍历序列构造二叉树
[力扣题目链接](https://leetcode.cn/problems/construct-binary-tree-from-preorder-and-inorder-traversal/description/)

---
## 📝数组作为函数的形式参数
数组作为函数的形式参数，传给函数的是数组的首元素的地址   
但是我们只知道起点，并不知道终点，还不能明确一个数组，因此，通常还要将数组的长度也传入函数   
我们可以这么认为：函数中一个数组由首元素地址和长度唯一确定   
于是我们只要对数组名(首元素地址)进行指针运算，再计算长度，就可以实现"分割数组"   
即将数组一分为二

# 🧠思路
从前序序列找根节点，分割中序序列   
1. 判断数组是否为空，如果为空，说明为空节点，返回NULL
2. 如果不为空，构建一个节点root，值为前序数组首元素的值
3. 在中序数组中查找root的值对应的下标index
4. 以index为界将中序数组一分为二(丢弃index对应的值)
5. 根据中序数组左右区间的长度，继续将前序数组一分为二(丢弃首元素)
6. 递归

完整代码：
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
struct TreeNode* buildTree(int* preorder, int preorderSize, int* inorder, int inorderSize) {
    //如果数组为空,为空节点
    if (inorderSize == 0) return NULL;
    //不为空，取前序第一个元素
    struct TreeNode* root = (struct TreeNode*)malloc(sizeof(struct TreeNode));
    root->val = *preorder;
    //找中序数组的分割点
    int index = 0;
    while (inorder[index] != root->val)
        index++;

    //递归
    //长度：         左  index      右 inorderSize-index-1
    //左中序数组起点地址：inorder    右中序数组起点地址：inorder+index+1
    //左前序数组起点地址：preorder+1 右前序数组起点地址：preorder+index+1
    root->left = buildTree(preorder+1, index, inorder, index);
    root->right = buildTree(preorder+index+1, inorderSize-index-1, inorder+index+1, inorderSize-index-1);

    return root;
}
```

# 🎉🎉🎉新年快乐
希望我们都能越来越好
