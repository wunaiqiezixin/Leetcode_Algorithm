# 111. 二叉树的最小深度
[力扣题目链接](https://leetcode.cn/problems/minimum-depth-of-binary-tree/description/)

---
二叉树的最小深度的定义：从根节点到最近叶子节点的最短路径上的节点数量。  
这里要特别注意**叶节点**：没有左右孩子的节点  
非常容易出错  
## 🍀递归
#### 分解问题的逻辑(后序遍历)---自底向上返回
- 即有返回值的递归   
二叉树的最小高度为：1 + (左子树的最小高度， 右子树的最小高度)    
注意：要保证左子树右子树的最小高度不能为0；因为如果为0，说明该节点不是叶子节点，不能拿他作为最小深度

### ***C:***
完整代码
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */

int minDepth(struct TreeNode* root) {
    //终止条件
    if (root == NULL) return 0;
    /* 左 */
    int leftMin = minDepth(root->left);
    /* 右 */
    int rightMin = minDepth(root->right);
    /* 中 */
    if (!leftMin && rightMin)
        return rightMin + 1;
    if (leftMin && !rightMin)
        return leftMin + 1;
    return 1 + (leftMin < rightMin ? leftMin : rightMin);
}
```
#### 遍历二叉树的逻辑
- 即没有返回值的递归
 
我们维护一个全局变量```minDepth```,即保证其生命周期和作用域在所有函数，但是力扣上要避免使用全局变量，于是我们需要一个指针来传递```minDepth```的值
需要遍历整棵树，因为要比较每一个叶节点的深度，从而得出最小值    

- 函数的参数     
```C
void traversal(struct TreeNode* node, int depth, int* minDepth);
//depth表示遍历到的节点的深度(注意回溯逻辑)
//minDepth用于将最小深度的值传递给力扣主函数
```

- 终止条件    

因为要遍历整棵树，所以节点为空时，直接返回      
```C
    //终止条件
    if (node == NULL) return;
```

- 递归遍历   

注意回溯逻辑
```C
    traversal(node->left, depth + 1, minDepth);//回溯逻辑

    traversal(node->right, depth + 1, minDepth);//回溯逻辑
```

### ***C:***
完整代码：
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
void traversal(struct TreeNode* node, int depth, int* minDepth)
{
    //终止条件
    if (node == NULL) return;

    if (!node->left && !node->right)//叶子节点(这几行代码随便插在哪)
    {
        if (depth < *minDepth)
            *minDepth = depth;
    }

    traversal(node->left, depth + 1, minDepth);//回溯逻辑

    traversal(node->right, depth + 1, minDepth);//回溯逻辑
    
    return;
}
int minDepth(struct TreeNode* root) {
    //空树深度为0
    if (root == NULL) return 0;

    int minDepth = 100000;
    traversal(root, 1, &minDepth);
    return minDepth;
}
```
## 🚀迭代法
层序遍历--直接套模板     
一层一层的遍历，当遇到第一个叶子节点时，返回深度  

### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
 //层序遍历：从左到右遇到第一个叶子节点返回深度
int minDepth(struct TreeNode* root) {
    if (root == NULL) return 0;//空树
    int depth = 0;//维护深度
    struct TreeNode** que = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 10000);//队列
    int front = 0, rear = 0;
    int size = 0;
    //根节点入队
    que[rear++] = root;
    //迭代
    while (front < rear)
    {
        depth++;
        size = rear - front;
        while (size--)
        {
            //出队
            struct TreeNode* cur = que[front++];
            if (!cur->left && !cur->right)
                return depth;
            //入队
            if (cur->left)
                que[rear++] = cur->left;
            if (cur->right)
                que[rear++] = cur->right;
        }
    }
    return 0;
}
```
