# 112. 路径总和
[力扣题目链接](https://leetcode.cn/problems/path-sum/description/)

---
## 🍀递归法
#### 🧠思路
本题以遍历二叉树为主，我们的递归函数不带返回值会更好写一点   
我们用一个指针ways表示符合条件的路径数量   

1. 函数的参数
```C
//无返回值:有时候加上返回值只会自找麻烦
//count初始为targetSum,每遍历到一个节点就减去它的值，ways表示符合条件的路径数
void traversal(struct TreeNode* node, int count, int* ways)
```
2. 终止条件：叶子节点且count为0
```C
    //终止条件
    if (!node->left && !node->right && count == 0)
    {
        (*ways)++;
        return;
    }
```
3. 递归遍历
```C
    //递归遍历：先左后右
    if (node->left)//回溯
    {
        count -= node->left->val;
        traversal(node->left, count, ways);
        count += node->left->val;
    }
    if (node->right)//回溯
    {
        count -= node->right->val;
        traversal(node->right, count, ways);
        count += node->right->val;
    }
    return;
```
完整代码：
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
//无返回值:有时候加上返回值只会自找麻烦
//count初始为targetSum,每遍历到一个节点就减去它的值，ways表示符合条件的路径数
void traversal(struct TreeNode* node, int count, int* ways)
{
    //终止条件
    if (!node->left && !node->right && count == 0)
    {
        (*ways)++;
        return;
    }
    //递归遍历：先左后右
    if (node->left)//回溯
    {
        count -= node->left->val;
        traversal(node->left, count, ways);
        count += node->left->val;
    }
    if (node->right)//回溯
    {
        count -= node->right->val;
        traversal(node->right, count, ways);
        count += node->right->val;
    }
    return;
}
bool hasPathSum(struct TreeNode* root, int targetSum) {
    int ways = 0;
    if (root == NULL) return ways;
    traversal(root, targetSum - root->val, &ways);
    return ways;
}
```
