# 225. 用队列实现栈
[力扣题目链接](https://leetcode.cn/problems/implement-stack-using-queues/description/)

---
## 🧠用一个队列实现栈
#### 思路1：用数组来实现循环队列
>维护一个front指针指向队列的顶部，维护size来表示队列的长度  
>队列的尾部指针指向队列最后一个元素的后方：rear = (front + size) % 数组大小  
>我们通过取余的操作实现对数组空间的充分利用  
### 📝代码实现
### ***C:***
```C

/*
用一个队列实现一个栈
队列用数组实现：
1. 用front指针指向队列的头部，用size记录队列的长度
2. 尾部指针 rear = front + size + 1
*/
typedef struct {
    int ar[100];
    int front;
    int size;
} MyStack;

//将front、size初始化为0
MyStack* myStackCreate() {
    MyStack* stack = (MyStack*)malloc(sizeof(MyStack));
    stack->front = 0;
    stack->size = 0;
    return stack;
}

/*
直接将元素加入到队列中
数组大小足够，不用考虑扩容
1. 计算出尾部指针：rear = front + size
2. 将元素添加到尾部
3. size加1
*/
void myStackPush(MyStack* obj, int x) {
    int rear = (obj->front + obj->size) % 100;
    obj->ar[rear] = x;
    obj->size++;
}

/*
1. 将队列前size-1个元素出队再入队
2. 将队列首元素出队，并记录其值返回
*/
int myStackPop(MyStack* obj) {
    int size = obj->size;
    size--;
    while (size--)
    {
        int rear = (obj->front + obj->size) % 100;
        obj->ar[rear] = obj->ar[obj->front];
        obj->front = (obj->front + 1) % 100;
    }
    int ret = obj->ar[obj->front];
    obj->front = (obj->front + 1) % 100;
    obj->size--;
    return ret;
}

//队列的尾部即为栈顶
int myStackTop(MyStack* obj) {
    int tail = (obj->front + obj->size) % 100;
    return obj->ar[--tail];
}

//长度为0，代表栈为空
bool myStackEmpty(MyStack* obj) {
    return obj->size == 0;
}

//将size和front重置为0
void myStackFree(MyStack* obj) {
    obj->size = 0;
    obj->front = 0;
}

/**
 * Your MyStack struct will be instantiated and called as such:
 * MyStack* obj = myStackCreate();
 * myStackPush(obj, x);
 
 * int param_2 = myStackPop(obj);
 
 * int param_3 = myStackTop(obj);
 
 * bool param_4 = myStackEmpty(obj);
 
 * myStackFree(obj);
*/
```
