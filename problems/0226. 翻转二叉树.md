# 226. 翻转二叉树
[力扣题目链接](https://leetcode.cn/problems/invert-binary-tree/description/)

---
## 🧠思路
我第一次看这个题目还有点懵  
我也知道它在考察层序遍历  
但就是因为我一直盯着整棵树看，捉不到本质  
其实不过就是，将每个节点的左右孩子交换了罢了  
看穿的话，这道题就很简单了
### 📝层序遍历
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
//层序遍历，交换左右孩子
struct TreeNode* invertTree(struct TreeNode* root) {
    //队列
    struct TreeNode** que = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 100);
    int front = 0, rear = 0, size = 0;
    //层序遍历
    if (root == NULL) return root;
    que[rear++] = root;
    while (front < rear)
    {
        size = rear - front;
        while (size--)
        {
            struct TreeNode* cur = que[front++];
            //交换左右孩子(注意入队顺序)
            struct TreeNode* swap = cur->left;
            if (cur->left)
                que[rear++] = cur->left;
            if (cur->right)
                que[rear++] = cur->right;
            cur->left = cur->right;
            cur->right = swap;
        }
    }
    return root;
}
```
其实这道题，也可以前序、后序、中序遍历，只要交换左右孩子就行
### 📑递归
### 1. 分解问题(有返回值的递归)
确定函数和要传入的参数:  
返回根节点，传入根节点
```C
struct TreeNode* invertTree(struct TreeNode* root)
```
边界条件
```C
if (root == NULL) return root;
```
单层递归逻辑
```C
//交换左右孩子
    struct TreeNode* tmp = root->left;
    root->left = root->right;
    root->right = tmp;
    //反转左子树
    invertTree(root->left);
    //反转右子树
    invertTree(root->right);
```
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
/*递归遍历*/
struct TreeNode* invertTree(struct TreeNode* root) {
    //边界条件
    if (root == NULL) return root;
    //交换左右孩子
    struct TreeNode* tmp = root->left;
    root->left = root->right;
    root->right = tmp;
    //反转左子树
    invertTree(root->left);
    //反转右子树
    invertTree(root->right);
    return root;
}
```
另一种思考方式：自底向上  
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
struct TreeNode* invertTree(struct TreeNode* root) {
    if (root == NULL) return NULL;//要遍历整棵树,NULL返回

    struct TreeNode* left = invertTree(root->left);//反转左子树

    struct TreeNode* right = invertTree(root->right);//反转右子树

    root->left = right;//用左节点接住反转的右子树
    root->right = left;//用右节点节点接住反转的左子树

    return root;//返回
}
```
### 遍历递归(无返回值的递归)
应该是最先想到的方法，就是直接把二叉树递归遍历一遍就像遍历数组一样     
然后每遍历到一个节点就交换它的左右孩子，前序位置后序位置中序位置都行   
遍历一整棵树，将下面两行代码交换位置也行  
```C
//交换孩子
traversal(node->left);
//在这交换孩子也行
traversal(node->right);
//在这交换孩子还是可以
```
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
void traversal(struct TreeNode* node)
{
    if (node == NULL) return;

    struct TreeNode* tmp = node->left;
    node->left = node->right;
    node->right = tmp;

    traversal(node->right);
    traversal(node->left);

    return;
}
struct TreeNode* invertTree(struct TreeNode* root) {
    traversal(root);
    return root;
}
```
### 🏖️迭代法遍历二叉树--前序遍历
用栈来代替递归
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
 //迭代法：前序遍历
struct TreeNode* invertTree(struct TreeNode* root) {
    //栈
    struct TreeNode** stack = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 100);
    int stackTop = 0;
    //根节点入栈
    if (root == NULL) return root;
    stack[stackTop++] = root;
    //迭代
    while (stackTop)
    {
        //弹出栈顶元素,交换左右孩子
        struct TreeNode* cur = stack[--stackTop];
        struct TreeNode* tmp = cur->left;
        cur->left = cur->right;
        cur->right = tmp;
        //左孩子入栈，右孩子入栈
        if (cur->left) stack[stackTop++] = cur->left;
        if (cur->right) stack[stackTop++] = cur->right;
    }
    return root;
}
```

