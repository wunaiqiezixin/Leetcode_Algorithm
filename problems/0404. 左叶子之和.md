# 404. 左叶子之和
[力扣题目链接](https://leetcode.cn/problems/sum-of-left-leaves/description/)

---
## 🍀递归
1. 函数参数与返回值
```C
int sumOfLeftLeaves(struct TreeNode* root);
//传入一个节点，返回树的左叶子节点的和
```
2. 终止条件
```C
if (root == NULL) return 0;//节点为空，返回0
if (!root->left && !root->right) return 0;//叶子节点，返回0
```
3. 单层递归逻辑
```C
int leftValues = sumOfLeftLeaves(root->left);  /*左*/
if (root->left && !root->left->left && !root->left->right) 
    leftValues = root->left->val; 
int rightValues = sumOfLeftLeaves(root->right); /*右*/
return leftValues + rightValues; /*中*/
```
完整代码：
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
int sumOfLeftLeaves(struct TreeNode* root) {
    //终止条件
    if (root == NULL) return 0;
    if (!root->left && !root->right) return 0;
    //递归
    int leftValues = sumOfLeftLeaves(root->left);/*左*/
    if (root->left && !root->left->left && !root->left->right)
        leftValues = root->left->val;
    int rightValues = sumOfLeftLeaves(root->right);/*右*/
    return leftValues + rightValues;/*中*/
}
```
