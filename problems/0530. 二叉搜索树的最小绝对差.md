# 530. 二叉搜索树的最小绝对差
[力扣题目链接](https://leetcode.cn/problems/minimum-absolute-difference-in-bst/description/)

---
## 🧠思路
#### 二叉搜索树中序遍历有序
最小绝对差只会出现在中序遍历序列的相邻两个元素间    

- 双指针法

我们只遍历二叉树一次就要得出答案
定义一个pre指针指向正在访问的前一个节点
将pre初始化为NULL

### 🍀递归法
- 力扣中的递归

注意不要将pre指针定义为全局变量，至少C语言是这样
因为力扣的判题系统会连续运行测试用例，**但是不是每次都会重置全局变量的值** ， 所以当我们通过一个用例后，再进行下一个用例时，pre还指向前一棵树的最后一个节点
```C
// 第一次测试用例：树A
isValidBST(treeA);  // 调用后，全局变量 pre 指向 treeA 的最后一个节点

// 第二次测试用例：树B
isValidBST(treeB);  // 调用时，pre 仍然指向 treeA 的节点，导致判断错误！
```
因此，我们将指针pre定义在力扣给的主函数isValidBST()中，当我们在其他函数需要用到时，直接将它的地址传入函数即可   


完整代码：
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */

void getMin(struct TreeNode* node, struct TreeNode** pre, int* minD)
{
    //终止条件
    if (node == NULL) 
        return;
    //左
    getMin(node->left, pre, minD);
    //中
    if (*pre)
    {
        int D = node->val - (*pre)->val;
        if (*minD > D)
            *minD = D;
    }

    *pre = node;
    //右
    getMin(node->right, pre, minD);
    return;

}
int getMinimumDifference(struct TreeNode* root) {
    struct TreeNode* pre = NULL;//定义在函数内部
    int minD = 1000000;
    getMin(root, &pre, &minD);
    return minD;
}
```
### 🚀迭代法
完整代码：
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
int getMinimumDifference(struct TreeNode* root) {
    //栈
    struct TreeNode** stack = (struct TreeNode**)malloc(sizeof(struct TreeNode*) * 10000);
    int stackTop = 0;
    int minD = 1000000;//结果
    struct TreeNode* cur = root;//用于遍历二叉树
    struct TreeNode* pre = NULL;//记录前一个节点
    //迭代
    while (stackTop || cur)
    {
        if (cur)
        {
            stack[stackTop++] = cur;
            //左
            cur = cur->left;
        }
        else
        {
            cur = stack[--stackTop];
            //中
            if (pre)//不为第一个元素
            {
                int D = cur->val - pre->val;
                if (D < minD)
                    minD = D;
            }

            pre = cur;
            //右
            cur = cur->right;
        }
    }
    return minD;
}
```
