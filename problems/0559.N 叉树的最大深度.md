# 559.N 叉树的最大深度
[力扣题目链接]()

---
思路：[0104.二叉树的最大深度](https://github.com/wunaiqiezixin/Leetcode_Algorithm/blob/main/problems/0104.%20%E4%BA%8C%E5%8F%89%E6%A0%91%E7%9A%84%E6%9C%80%E5%A4%A7%E6%B7%B1%E5%BA%A6.md)  
## 🍀递归
### 后序遍历
### ***C:***
```C
/**
 * Definition for a Node.
 * struct Node {
 *     int val;
 *     int numChildren;
 *     struct Node** children;
 * };
 */
//后序遍历：左-右-中
int maxDepth(struct Node* root) {
    if (root == NULL) return 0;
    int mx = 0;
    for (int i = 0; i < root->numChildren; i++)
    {
        int depth = maxDepth(root->children[i]);
        if (depth > mx)
            mx = depth;
    }
    return mx + 1;
}
```
### 中序遍历---回溯
```C
/**
 * Definition for a Node.
 * struct Node {
 *     int val;
 *     int numChildren;
 *     struct Node** children;
 * };
 */
//前序遍历：中-左右
int result;
void getDepth(struct Node* node, int depth)
{
    /*中*/
    result = result > depth ? result : depth;
    if (node->numChildren == 0) return;
    /*从左到右*/
    for (int i = 0; i < node->numChildren; i++)
    {
        struct Node* child = node->children[i];
        depth++;
        getDepth(child, depth);
        depth--;//回溯
    }
    return;
}
int maxDepth(struct Node* root) {
    result = 0;
    if (root == NULL) return result;
    getDepth(root, 1);
    return result;
}
```
### 📝迭代
### 层序遍历
### ***C:***
```C
/**
 * Definition for a Node.
 * struct Node {
 *     int val;
 *     int numChildren;
 *     struct Node** children;
 * };
 */

int maxDepth(struct Node* root) {
    //队列
    struct Node** que = (struct Node**)malloc(sizeof(struct Node*) * 10000);
    int front = 0, rear = 0;
    //迭代
    int size;
    int depth = 0;
    if (root == NULL) return 0;
    que[rear++] = root;
    while (front < rear)
    {
        depth++;
        size = rear - front;
        while (size--)
        {
            //出队
            struct Node* cur = que[front++];
            for (int i = 0; i < cur->numChildren; i++)
            {
                //入队
                que[rear++] = cur->children[i];
            }
        }
    }
    return depth;
}
```
