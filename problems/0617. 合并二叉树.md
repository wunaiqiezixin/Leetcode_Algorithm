# 617. 合并二叉树
[力扣题目链接](https://leetcode.cn/problems/merge-two-binary-trees/description/)   

---
我们要同时遍历两颗二叉树，我们是怎么样做到的呢    
其实很简单，就是把两颗树的根节点作为参数传入一个函数

## 🍀递归
破坏root1的结构   
如果root1和root2都不为空，将root2的值累加到root1节点上   
如果root1不存在， 直接返回root2   
如果root2不存在， 直接返回root1

完整代码：
### ***C:***
```C
/**
 * Definition for a binary tree node.
 * struct TreeNode {
 *     int val;
 *     struct TreeNode *left;
 *     struct TreeNode *right;
 * };
 */
struct TreeNode* mergeTrees(struct TreeNode* root1, struct TreeNode* root2) {
    //终止条件
    if (root1 == NULL) 
        return root2;
    if (root2 == NULL)
        return root1;

    //如果root1和root2均不为空
    root1->val += root2->val;//将值加到root1
    root1->left = mergeTrees(root1->left, root2->left);
    root1->right = mergeTrees(root1->right, root2->right);

    return root1;
}
```
